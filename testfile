/* The initial program
	
*/

1+1*2 && 7*8

program
{
	int _test_val, hello, woo;
	int should_err, probably;
	
	while(i < 200)
	{
		print("\"hello\"\n");
		// print("comment\n");
	}
	i += -1;
	i -= 0x12A74AD;
}
