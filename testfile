/* The initial program
	
*/

program
{
	int _test_val, hello, woo;
	int should_err, probably, i;
	char aChar;
	string aString, bString;

	struct myStruct {
		int a;
		int b;

		char f;
	};
	
	while(i < 200)
	{
		print("\"hello\"\n");
		// print("comment\n");
	}
	_test_val = 100;
	i += -1;
	i -= 0x12A74AD;

	aChar = '\n';

	aString = "hello";

	if ( i < 0)
	{
		exit(0);
	}

	else
	{
		print("all good\n", 1+1, "forU");
	}
}
