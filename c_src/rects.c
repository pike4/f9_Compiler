#include <stdio.h>
#include <stdlib.h>
#include<string.h>

int main() {

	int w = 0, h = 0 , x = 0 , y = 0 ;
	char* temp0 = "Enter an integer for the width and the height\n";
	printf("%s",temp0);
	scanf("%d%d",&w,&h);
	while( x  < 